version: '3.8' 

# liste des services
services:
  # mon premier service
  app_flask:
    #l'image
    image: my_app
    #les ports
    ports:
      - 5000:5000
    #les dépendances
    depends_on: 
      - mymongodb


  # seconde service
  mymongodb:
    image: mongo
    ports:
      - 27017:27017